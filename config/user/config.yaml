postgres:
  url: "host=postgres port=5432 user=postgres password=Pa$w0rd dbname=graduation_thesis sslmode=disable application_name=user_service"

redis:
  url: redis://redis:6379/0

authenticator:
  url: http://authenticator:8085/v1/validate

token:
  at_expires: 60*15 # 15 mins
  rt_expires: 60*60*24 # 1 day
  access_secret: 4cC3Ss_s3CrEt
  refresh_secret: tErC3s_hS3rfer

app:
  https_port: 8098
  http_port: 18098
  key:
  cert: 
